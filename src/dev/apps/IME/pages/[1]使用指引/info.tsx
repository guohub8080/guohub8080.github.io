import { BookOpen } from 'lucide-react';
import type { CategoryInfo } from '../data/imeLoader.tsx';

const categoryInfo: CategoryInfo = {
  slug: 'getting-started',
  icon: BookOpen
};

export default categoryInfo;

