import { FileText } from 'lucide-react';
import type { CategoryInfo } from '../data/planTodoLoader.tsx';

const categoryInfo: CategoryInfo = {
  slug: 'panel',
  icon: FileText
};

export default categoryInfo;

