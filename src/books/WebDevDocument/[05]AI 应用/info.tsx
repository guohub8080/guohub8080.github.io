import { Bot } from "lucide-react";
import type { CategoryInfo } from '../data/webDevDocumentLoader.tsx';

const categoryInfo: CategoryInfo = {
  icon: Bot,
  slug: "ai-application"
};

export default categoryInfo;

